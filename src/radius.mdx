import { Meta, Title } from "@storybook/addon-docs/blocks";
import { radius } from "./tokens/radius";
import React from "react";

<Meta title="Tokens/Radius" />

<Title>Border Radius</Title>

Border radius tokens define the roundedness of corners for UI elements.

<div className="grid grid-cols-[8rem_10rem_5rem] gap-x-4 gap-y-2 items-center">
  {Object.entries(radius).map(([name, value]) => (
    <React.Fragment key={name}>
      <div className="text-sm font-medium">{name}</div>
      <div className="text-sm font-mono text-slate-600">{value}</div>
      <div
        className="w-20 h-20 bg-primary"
        style={{ borderRadius: value }}
      />
    </React.Fragment>
  ))}
</div>


