import {
  Meta,
  Title,
  IconGallery,
  IconItem,
} from "@storybook/addon-docs/blocks";
import { icons, Icon } from "./react/components/icon";

<Meta title="Tokens/Icons" />

<Title>Icons</Title>

These are the icons being used in our design system. They are available as React components.

<IconGallery>
  {icons.map((icon) => (
    <IconItem key={icon} name={icon}>
      <Icon type={icon} className="text-primary" />
    </IconItem>
  ))}
</IconGallery>
