import { Meta, Title, Primary, Stories, Controls } from "@storybook/addon-docs/blocks";
import InputStories from "./input.stories";

<Title>Input</Title>

<Meta of={InputStories} />

Input fields allow users to enter text data. Supports currency formatting, validation states, and multiple sizes.

<Primary />

<Controls />

## Currency Input

Enable currency formatting by setting `currency={true}`. The input will automatically format numbers with two decimal places and display a currency symbol.

## Variants

- **default**: Standard input styling
- **error**: Red border and focus ring for validation errors

## Sizes

Available sizes: `sm`, `md`, `lg`

## Accessibility

Always pair inputs with labels. Use the `error` variant to indicate validation failures.

<Stories />

