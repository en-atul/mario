import { Meta, Title } from "@storybook/addon-docs/blocks";
import { elevation } from "./tokens/elevation";

<Meta title="Tokens/Elevation" />

<Title>Elevation (Shadows)</Title>

Elevation tokens define box shadows to create depth and hierarchy in the UI.

<div className="space-y-6">
  {Object.entries(elevation).map(([name, value]) => (
    <div key={name} className="flex items-center gap-6">
      <div className="w-32 text-sm font-medium">{name}</div>
      <div className="text-xs text-slate-600 font-mono max-w-md break-all">{value}</div>
      <div
        className="w-32 h-32 bg-white rounded-lg flex items-center justify-center"
        style={{ boxShadow: value }}
      >
        <span className="text-slate-400 text-sm">{name}</span>
      </div>
    </div>
  ))}
</div>

